HUNTSCOUT Claude Code Build Spec INTERNAL TECHNICAL DOCUMENT Stack: Next.js · Supabase · Vercel · Stripe · Claude API Workflow: Claude Code in terminal · Blank Next.js scaffold May 2026 — Working Draft How Claude Code Works Claude Code runs in your terminal and reads your entire codebase. It can create files, edit files, run commands, and install packages — all from a prompt you type or paste. This doc gives you the exact prompts to use for each step of the HuntScout build. The Golden Rules One task per session. &apos;Build the marketplace&apos; will produce garbage. &apos;Build the outfitter listing create form that saves to Supabase&apos; will produce something you can test. Always start a new session by telling Claude Code what the project is and what you want today. It has no memory between sessions. If it breaks something, say: &apos;That broke X — revert that change and try a different approach.&apos; If output looks wrong, say: &apos;Show me what files you changed.&apos; Then review them. End every session with: &apos;Summarize what was built and what the next step is.&apos; Paste that into a note. Session Opener — Paste This Every Time Copy this block and paste it at the start of every Claude Code session. It gives Claude the full context it needs without you having to re-explain the project. We are building HuntScout — a guided hunt marketplace and AI draw strategy tool. Stack: Next.js 14 (App Router), Supabase (PostgreSQL + Auth + Storage), Vercel (hosting), Stripe (payments), Anthropic Claude API (Scout AI). Two products: 1. Marketplace: hunters search and book guided hunts. Outfitters list their operations. 2. Scout: hunters enter preference points and get draw odds data (free) + AI strategy conversation (paid). Today I want to: [DESCRIBE THE TASK] Before doing anything, read the CONTEXT.md file in the root of the project. The CONTEXT.md file is the single most important thing in the repo. We create it in Setup Step 3. Claude Code reads it at the start of every session to understand what&apos;s been built and what&apos;s next. Setup — Before You Write a Single Line of Code Do every step in this section before starting any feature build. These are the accounts, config, and files that everything else depends on. Accounts to Create First Service Go To What to Do Free? Supabase supabase.com Create account, then New Project — name it &apos;huntscout&apos;. Save the project URL and anon key. Yes Vercel vercel.com Create account, connect your GitHub. You&apos;ll deploy from here. Yes Stripe stripe.com Create account. Go to Developers → API Keys. Save the publishable and secret keys. Yes (fees per txn) Anthropic console.anthropic.com Create account, generate an API key under API Keys. This powers Scout AI. Pay-per-use Cloudflare cloudflare.com/registrar Buy huntscout.com here (~$9/yr). Also grab huntscoutapp.com as backup. $9/yr Resend resend.com Create account for transactional email (booking confirmations, alerts). 100/mo free Step 1 — Install Claude Code If you haven&apos;t installed Claude Code yet, run this in your terminal. $ npm install -g @anthropic-ai/claude-code $ claude --version ✓ Check: You see a version number printed. If not, restart your terminal and try again. Step 2 — Set Up the Next.js Project Navigate to where you want the project folder, then run this. Choose: TypeScript → Yes, ESLint → Yes, Tailwind → Yes, src/ directory → No, App Router → Yes. $ npx create-next-app@latest huntscout $ cd huntscout $ npm install @supabase/supabase-js @supabase/auth-helpers-nextjs stripe @stripe/stripe-js ✓ Check: Running &apos;npm run dev&apos; opens localhost:3000 with the default Next.js page. Step 3 — Create the CONTEXT.md File This is the most important file in the project. Claude Code reads it at the start of every session. Create it in the root of the project manually or paste this into Claude Code: Create a file called CONTEXT.md in the root of this project with this exact content: # HuntScout — Project Context ## What This Is HuntScout is a two-sided platform: 1. Marketplace — hunters search/book guided hunts, outfitters list their operations 2. Scout — draw odds table (free) + AI strategy conversation (paid via Stripe subscription) ## Stack - Frontend: Next.js 14, App Router, Tailwind CSS - Database/Auth: Supabase (PostgreSQL + Supabase Auth) - Hosting: Vercel - Payments: Stripe (bookings = one-time charge, Scout = subscription) - AI: Anthropic Claude API (Scout conversation layer) - Email: Resend ## Build Status [UPDATE THIS AFTER EVERY SESSION] - [ ] Environment setup - [ ] Database schema - [ ] Auth - [ ] Outfitter listing create - [ ] Hunter listing search - [ ] Booking flow - [ ] Scout draw odds table - [ ] Scout AI layer ## Decisions Made - Commission: 10% per booking (7% for founding outfitters) - Scout free: draw odds table only - Scout paid: $9.99/mo or $59.99/yr — AI conversation + multi-year modeling - Data source: FOIA from state game agencies (not GoHunt) - Launch states: CO, WY, MT, UT, ID, AZ, NV ✓ Check: CONTEXT.md exists in the project root and is readable. Step 4 — Environment Variables Create a .env.local file in the project root. Never commit this file — it should already be in .gitignore. Create a .env.local file in the root with these variables (I will fill in the values): NEXT_PUBLIC_SUPABASE_URL= NEXT_PUBLIC_SUPABASE_ANON_KEY= SUPABASE_SERVICE_ROLE_KEY= STRIPE_SECRET_KEY= NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY= STRIPE_WEBHOOK_SECRET= ANTHROPIC_API_KEY= RESEND_API_KEY= NEXT_PUBLIC_APP_URL=http://localhost:3000 Then fill in each value from the accounts you created in the table above. ✓ Check: Running &apos;npm run dev&apos; still works. No errors about missing environment variables. Step 5 — Connect Supabase This creates the Supabase client that every part of the app uses to talk to the database. Create a Supabase client utility. Set up two files: 1. lib/supabase/client.ts — browser-side Supabase client using NEXT_PUBLIC_ env vars 2. lib/supabase/server.ts — server-side Supabase client using the service role key for admin operations Use the @supabase/auth-helpers-nextjs package for the server client. After creating them, add a test: in the root page.tsx, temporarily fetch the count of rows from a table called &apos;listings&apos; and log it to console. We expect an error about the table not existing yet — that confirms the connection works. ✓ Check: The browser console or terminal shows a Supabase error about &apos;listings&apos; not existing — this means the connection is working. Remove the test code after confirming. Step 6 — Deploy to Vercel Get a live URL before building features. Do this once now, then every push deploys automatically. $ git add . &amp;&amp; git commit -m &apos;Initial setup&apos; $ git push origin main Then go to vercel.com, click &apos;Add New Project&apos;, import your GitHub repo. Add all the environment variables from .env.local in the Vercel dashboard under Settings → Environment Variables. Deploy. ✓ Check: Your live Vercel URL loads the default Next.js page without errors. The URL will look like huntscout-xyz.vercel.app. Add ALL environment variables to Vercel now — not just some. Missing env vars in production are one of the most common build failures. Phase 1A — Database Schema The schema is the foundation. Every feature reads and writes from these tables. Build it correctly once before touching any UI. Master Schema Prompt Paste this entire prompt into Claude Code as a single task. It builds all tables, foreign keys, indexes, and Row Level Security in one session. Read CONTEXT.md first. Build the complete Supabase database schema for HuntScout by running SQL in the Supabase dashboard (give me the SQL to copy-paste, not code that runs it programmatically). Tables needed: users id uuid primary key (references auth.users) email text unique not null full_name text role text check (role in (&apos;hunter&apos;,&apos;outfitter&apos;,&apos;admin&apos;)) default &apos;hunter&apos; created_at timestamptz default now() outfitter_profiles id uuid primary key default gen_random_uuid() user_id uuid references users(id) on delete cascade business_name text not null state text not null license_number text license_verified boolean default false years_in_operation integer bio text success_rate numeric(5,2) guide_ratio text response_time_hours integer instant_booking_enabled boolean default false founding_outfitter boolean default false commission_rate numeric(4,2) default 10.00 listings id uuid primary key default gen_random_uuid() outfitter_id uuid references outfitter_profiles(id) on delete cascade title text not null species text[] not null states text[] not null weapon_types text[] hunt_style text access_type text check (access_type in (&apos;public&apos;,&apos;private&apos;,&apos;high_fence&apos;,&apos;mixed&apos;)) terrain_difficulty integer check (terrain_difficulty between 1 and 5) duration_days integer group_size_min integer default 1 group_size_max integer lodging_type text base_price numeric(10,2) not null deposit_amount numeric(10,2) description text success_rate_override numeric(5,2) is_combination_hunt boolean default false ada_accessible boolean default false points_required boolean default false is_active boolean default true created_at timestamptz default now() bookings id uuid primary key default gen_random_uuid() listing_id uuid references listings(id) hunter_id uuid references users(id) outfitter_id uuid references outfitter_profiles(id) status text check (status in (&apos;pending&apos;,&apos;confirmed&apos;,&apos;completed&apos;,&apos;cancelled&apos;)) default &apos;pending&apos; start_date date not null end_date date not null party_size integer default 1 total_price numeric(10,2) deposit_paid boolean default false deposit_amount numeric(10,2) booking_type text check (booking_type in (&apos;instant&apos;,&apos;request&apos;)) not null stripe_payment_intent_id text created_at timestamptz default now() reviews id uuid primary key default gen_random_uuid() booking_id uuid references bookings(id) hunter_id uuid references users(id) outfitter_id uuid references outfitter_profiles(id) rating integer check (rating between 1 and 5) not null body text photos text[] created_at timestamptz default now() draw_odds id uuid primary key default gen_random_uuid() state text not null species text not null unit_number text not null weapon_type text not null year integer not null total_applicants integer successful_draws integer draw_odds_percent numeric(5,2) avg_points_drawn numeric(5,1) min_points_drawn integer scout_queries id uuid primary key default gen_random_uuid() user_id uuid references users(id) state text not null species text not null points integer not null weapon_type text priority_weights jsonb created_at timestamptz default now() Add: - Indexes on: listings(outfitter_id), listings(species), listings(states), bookings(hunter_id), bookings(outfitter_id), draw_odds(state, species, year) - Row Level Security on all tables - RLS policies: hunters see only their own bookings and scout_queries; outfitters only edit their own listings and outfitter_profiles; draw_odds is public read; reviews are public read Output the complete SQL ready to paste into the Supabase SQL editor. ✓ Check: Go to Supabase dashboard → Table Editor. You should see all 7 tables listed: users, outfitter_profiles, listings, bookings, reviews, draw_odds, scout_queries. Run the SQL in the Supabase SQL editor (Database → SQL Editor → New Query). Don&apos;t ask Claude Code to run it programmatically — paste it yourself so you can see if anything errors. Phase 1B — Authentication Auth comes before any UI. Every page needs to know who&apos;s logged in and what role they have. Build this once and it works everywhere. Master Auth Prompt This builds sign up, login, logout, and role-based routing in one session. Read CONTEXT.md first. Build authentication for HuntScout using Supabase Auth. Requirements: 1. Sign up page at /signup — email + password + full name + role selector (Hunter or Outfitter). On signup, create a row in the public.users table with the role. Redirect hunters to /dashboard, outfitters to /outfitter/dashboard. 2. Login page at /login — email + password. Redirect based on role after login. 3. Logout button — clears session, redirects to /login. 4. A middleware.ts file in the root that: - Redirects unauthenticated users away from /dashboard and /outfitter/* to /login - Redirects hunters trying to access /outfitter/* to /dashboard - Allows unauthenticated access to /, /listings/*, /login, /signup, /scout 5. A useUser() hook that returns the current user object and their role from the users table. Use Supabase Auth email/password provider. Style forms with Tailwind — clean, minimal, dark green primary color (#1B4332). ✓ Check: You can sign up as both a Hunter and an Outfitter. After login, each role lands on a different dashboard URL. Visiting /outfitter/dashboard while logged in as a hunter redirects you away. Follow-up: Protect API Routes Any API route that writes data needs server-side auth. Run this after the main auth prompt. Add a helper function at lib/auth/requireUser.ts that: - Gets the current session server-side using the Supabase server client - Returns the user and their role from the users table - Throws a 401 response if no valid session exists Show me how to use it in a Next.js API route example. ✓ Check: Calling the API route without a session returns a 401. Calling it while logged in returns 200. Phase 1C — Marketplace Build in this order: outfitter listing create → hunter search → listing detail → booking flow. Each step depends on the previous. 5.1 — Outfitter Listing Create Outfitters need to be able to create their hunt listings. This is the supply side of the marketplace. Read CONTEXT.md first. Build the outfitter listing creation flow at /outfitter/listings/new. Requirements: Form fields (all saved to the listings table): - Title (text) - Species (multi-select: elk, mule deer, whitetail, pronghorn, bear, bighorn sheep, mountain goat) - States (multi-select: CO, WY, MT, UT, ID, AZ, NV) - Weapon types (multi-select: rifle, archery, muzzleloader, crossbow) - Hunt style (select: spot-and-stalk, stand hunting, calling, dog hunting) - Access type (select: public, private, high_fence, mixed) - Terrain difficulty (slider 1–5 with labels: Easy / Moderate / Strenuous / Pack-in / Extreme) - Duration in days (number) - Group size min and max (two number inputs) - Lodging type (select: spike camp, tent camp, wall tent, lodge, ranch house, cabin) - Base price in dollars (number) - Deposit amount (number) - Description (textarea) - Success rate override % (number, optional) - Combination hunt toggle (boolean) - ADA accessible toggle (boolean) - Points required toggle (boolean) - Booking type (radio: instant or request) On submit: insert to listings table with the outfitter_id from the current user&apos;s outfitter_profile. Show a success message and redirect to /outfitter/listings. Also build /outfitter/listings — a simple table showing this outfitter&apos;s listings with title, species, price, status (active/inactive), and an Edit link. ✓ Check: Log in as an outfitter. Fill out the form and submit. Go to Supabase → Table Editor → listings. The new listing row should appear with all fields populated correctly. 5.2 — Hunter Listing Search The hunter-facing search page. This is the first thing a hunter sees. Read CONTEXT.md first. Build the hunter listing search page at /listings. Requirements: Filters sidebar (left side): - Species (multi-select checkboxes) - State (multi-select checkboxes: CO, WY, MT, UT, ID, AZ, NV) - Weapon type (multi-select checkboxes) - Price range (min/max number inputs) - Access type (checkboxes: public, private — exclude high_fence unless checked) - Duration (select: any, 1-3 days, 4-6 days, 7+ days) - ADA accessible (toggle — only show accessible listings) - OTC only (toggle — only show listings where points_required = false) Results area (right side): - Listing cards showing: title, outfitter name, species tags, state, price, duration, terrain difficulty badge, success rate if available - &apos;Book Now&apos; vs &apos;Request to Book&apos; badge based on booking_type - Results update without page reload when filters change - Show result count Query: filter the listings table using the selected filters. Only show is_active = true listings. Make the page work without being logged in — hunters should be able to browse before signing up. ✓ Check: Open /listings. You see any listings you created as the outfitter. Changing filters updates results. The page loads while logged out. 5.3 — Listing Detail Page The full listing view a hunter sees before deciding to book. Read CONTEXT.md first. Build the listing detail page at /listings/[id]. Requirements: Display all listing fields in a clean layout: - Hero section: title, outfitter name + state, species tags, price - Details grid: duration, group size, lodging, terrain, access type, weapon types, ADA badge if applicable - Description section - Success rate (show outfitter&apos;s override if set, otherwise their profile success_rate) - Outfitter info sidebar: business name, years in operation, guide ratio, response time, license verified badge - Reviews section: show all reviews for this outfitter&apos;s listings, average rating - Booking CTA button: &apos;Book Now&apos; (if instant) or &apos;Request to Book&apos; (if request) — link to /listings/[id]/book - If user is not logged in, CTA says &apos;Sign Up to Book&apos; Fetch listing by id from Supabase, join with outfitter_profiles, fetch reviews separately. ✓ Check: Clicking a listing card from /listings opens the detail page. All fields display. The booking button shows the correct label based on booking_type. 5.4 — Booking Flow The most critical flow in the app. Build the request model first, then instant. Read CONTEXT.md first. Build the booking flow at /listings/[id]/book. Build the REQUEST model first. Step 1 — Booking form: - Hunt start date (date picker) - Hunt end date (auto-calculated from duration, but editable) - Party size (number, between listing&apos;s group_size_min and group_size_max) - Show: total price = base_price × party_size, deposit = deposit_amount - Message to outfitter (textarea, optional) - Submit button: &apos;Request to Book&apos; On submit: - Create a booking row with status = &apos;pending&apos;, booking_type = &apos;request&apos; - Send email to outfitter via Resend: &apos;You have a new booking request from [hunter name] for [listing title] on [dates]&apos; - Send email to hunter: &apos;Your request has been sent. The outfitter will respond within 48 hours.&apos; - Redirect to /dashboard/bookings Also build the outfitter side at /outfitter/bookings: - List of all pending booking requests - &apos;Confirm&apos; and &apos;Decline&apos; buttons on each - Confirming a request: update status to &apos;confirmed&apos;, email the hunter Do not add Stripe yet — we&apos;ll add deposit collection as a follow-up prompt. ✓ Check: Submit a booking request as a hunter. Check Supabase → bookings table — a row appears with status &apos;pending&apos;. Log in as the outfitter, go to /outfitter/bookings, and click Confirm. The status updates to &apos;confirmed&apos; in Supabase. 5.5 — Stripe Deposit Add Stripe to collect the deposit when a booking is confirmed. Run this after 5.4 is working. Read CONTEXT.md first. Add Stripe deposit collection to the booking flow. For REQUEST bookings: - After the outfitter confirms, trigger a Stripe Checkout session for the deposit amount - Email the hunter with a payment link: &apos;Your hunt is confirmed! Complete your deposit to secure your spot.&apos; - On successful payment: update bookings.deposit_paid = true, store the stripe_payment_intent_id For INSTANT bookings (add this variant to the booking form): - After the hunter fills in dates and party size, redirect to Stripe Checkout immediately - On successful payment: create the booking row with status = &apos;confirmed&apos; and deposit_paid = true Build a Stripe webhook handler at /api/webhooks/stripe that: - Listens for checkout.session.completed - Updates the relevant booking row in Supabase - Sends confirmation emails to both parties via Resend Use Stripe test mode keys from .env.local. Show me how to test the webhook locally using the Stripe CLI. ✓ Check: In Stripe test mode, complete a payment using card number 4242 4242 4242 4242. Check Supabase — deposit_paid updates to true. Check your email — confirmation arrives for both hunter and outfitter. 5.6 — Review System Triggered automatically after a hunt ends. Keep this simple for now. Read CONTEXT.md first. Build the review system. 1. A Supabase scheduled function (or a Next.js API route we&apos;ll trigger manually for now) that: - Finds all bookings where status = &apos;confirmed&apos; and end_date &lt; today and deposit_paid = true - Sends a review request email to the hunter via Resend with a link to /listings/[listing_id]/review 2. Review form at /listings/[id]/review (auth required): - Star rating 1–5 - Written review (textarea) - Submit → insert to reviews table - One review per booking (check booking_id is unique in reviews) 3. Reviews display on the listing detail page (already built) — confirm they pull through correctly. ✓ Check: Submit a review on a listing. Go to Supabase → reviews table. The row appears. Reload the listing detail page — the review shows up and the average rating updates. Phase 2A — Scout: Draw Odds Table (Free Tier) Scout free tier is a filterable draw odds table built from FOIA state data. No AI yet — just fast, clean data display. Phase 2B adds the Claude API conversation layer. 6.1 — FOIA Data Import Once you receive draw results CSVs from state game agencies, use this prompt to import them. Run once per state received. Read CONTEXT.md first. I have received draw results data from [STATE] game agency. The file is at [FILE PATH]. The columns in this file are: [PASTE ACTUAL COLUMN NAMES FROM THE CSV] Write a Node.js script that: 1. Reads this CSV file 2. Maps the columns to our draw_odds table schema: state, species, unit_number, weapon_type, year, total_applicants, successful_draws, draw_odds_percent, avg_points_drawn, min_points_drawn 3. Cleans any formatting issues (remove commas from numbers, standardize species names to match our species list, handle blank cells) 4. Upserts rows to Supabase draw_odds table — if a row with the same state+species+unit_number+weapon_type+year exists, update it instead of duplicating 5. Logs how many rows were inserted vs updated vs skipped Run the script and show me the output. ✓ Check: Go to Supabase → draw_odds table. Rows exist for the imported state. Filter by state to confirm. Check that draw_odds_percent values look reasonable (0–100). 6.2 — Scout Page (Free Tier) The main Scout page — accessible without login. Shows the draw odds table with filters. Read CONTEXT.md first. Build the Scout page at /scout. This is the free tier — no auth required. Filter controls at top: - State (select: CO, WY, MT, UT, ID, AZ, NV) - Species (select based on what&apos;s in draw_odds for that state) - Weapon type (select based on what&apos;s available) - My points (number input) - Year (select, default to most recent year in the data) Results table columns: - Unit - Draw Odds % - Avg Points to Draw - Min Points to Draw - Total Applicants - Draws Issued - Trend arrow (if prior year data exists: up = odds improving, down = tightening) Behavior: - When user enters their points, highlight rows where min_points_drawn &lt;= their points in green - Sort by draw_odds_percent descending by default - Allow column header click to re-sort - Show &apos;No data yet for this state&apos; if draw_odds has no rows for the selected state - Paywall banner at bottom: &apos;Get AI strategy analysis → Upgrade to Scout Pro&apos; — link to /scout/upgrade Query the draw_odds table server-side and pass data as props. ✓ Check: Go to /scout while logged out. Select Colorado, Elk, Rifle. The table populates. Enter a points value — matching rows highlight. Sorting by column header works. 6.3 — Scout Pro Subscription (Stripe) Gate the AI features behind a Stripe subscription before building the AI itself. Read CONTEXT.md first. Build Scout Pro subscription at /scout/upgrade. 1. Create two Stripe products in the dashboard manually: - Scout Pro Monthly: $9.99/month recurring - Scout Pro Annual: $59.99/year recurring Save both price IDs to .env.local as STRIPE_SCOUT_MONTHLY_PRICE_ID and STRIPE_SCOUT_ANNUAL_PRICE_ID 2. Build /scout/upgrade page: - Two plan cards: Monthly ($9.99/mo) and Annual ($59.99/yr — save 50%) - Subscribe button on each → Stripe Checkout in subscription mode - Require login before starting checkout 3. Add a scout_subscription field to the users table: - status text (none, active, cancelled) - stripe_customer_id text - stripe_subscription_id text - current_period_end timestamptz 4. Handle these Stripe webhook events in /api/webhooks/stripe: - customer.subscription.created → set status = active - customer.subscription.deleted → set status = cancelled 5. Create a helper isScoutPro(userId) that returns true if users.scout_subscription.status = active and current_period_end &gt; now() ✓ Check: Subscribe with test card 4242 4242 4242 4242. Check Supabase users table — scout_subscription.status updates to &apos;active&apos;. The isScoutPro() helper returns true for that user. Phase 2B — Scout AI Conversation Layer Build Phase 2A (draw odds table + Stripe subscription) completely before starting here. The AI layer sits on top of working data and working payments. 7.1 — Scoring Function The scoring function ranks units based on the hunter&apos;s priorities. It runs before the AI conversation to give Claude structured data to reason from. Read CONTEXT.md first. Build the Scout scoring function at lib/scout/score.ts. Input: state: string species: string points: number weaponType: string year: number weights: { drawOdds: number // 1-5 trophyQuality: number // 1-5 (proxy: use success_rate as signal) publicLandAccess: number // 1-5 (proxy: access_type = public listings in that unit) pointsBurnWillingness: number // 1-5 (high = ok to apply for hard units) } Logic: 1. Query draw_odds for all units matching state + species + weapon_type + year 2. For each unit calculate: - odds_score: draw_odds_percent normalized 0-1 - reachability_score: 1 if min_points_drawn &lt;= points, 0.5 if within 2 points, 0 if out of reach - composite_score: weighted average using the priority weights 3. Sort by composite_score descending 4. Return top 10 units with: unit_number, draw_odds_percent, avg_points_drawn, min_points_drawn, composite_score, a &apos;reachable_this_year&apos; boolean Export as a typed function. Write a simple test that calls it with hardcoded inputs and logs the result. ✓ Check: Running the test logs an array of 10 units sorted by composite_score. Units where min_points_drawn &lt;= your input points have reachable_this_year = true. 7.2 — Claude API Conversation The AI layer. Takes the scored unit list and enables a conversation about it. Read CONTEXT.md first. Build the Scout AI conversation at /api/scout/chat (POST route) and the chat UI on /scout when the user is Scout Pro. API route /api/scout/chat: 1. Check isScoutPro(userId) — return 403 if not subscribed 2. Accept: { state, species, points, weaponType, weights, messages: [{role, content}] } 3. Run the scoring function to get the top 10 units 4. Call the Anthropic Claude API (claude-sonnet-4-20250514) with: System prompt: &apos;You are a western big game draw strategy expert for HuntScout. The hunter has [points] preference points for [species] in [state] with a [weaponType] tag. Here is their scored unit list based on their priorities: [JSON of scored units]. Respond with specific, actionable advice. Reference unit numbers directly. Explain tradeoffs clearly. If asked about multi-year strategy, calculate using the trend data provided. Never make up unit numbers or odds not in the provided data.&apos; 5. Include full messages array for conversation continuity 6. Stream the response back using ReadableStream 7. Save the query to scout_queries table Chat UI on /scout (shown only to Scout Pro users, below the odds table): - Priority weight sliders (draw odds, trophy quality, public land, points burn willingness) - Chat input + message history - Streaming response display - First message auto-sent: &apos;Based on my [points] points, what unit should I apply for?&apos; Cache responses: if the same state+species+points+weaponType combo was queried in the last 24 hours, return the cached scored units (not the AI response — that should always be fresh). ✓ Check: Log in as a Scout Pro user. Go to /scout, select a state and species. The chat UI appears below the table. Type a question. A streaming response appears that references actual unit numbers from the draw_odds table. 7.3 — Multi-Year Modeling A follow-up prompt to add the &apos;what if I wait&apos; feature to the existing AI conversation. Read CONTEXT.md first. Add multi-year modeling to the Scout scoring function and AI system prompt. In the scoring function, add a multi_year_projection for each unit: - Query draw_odds for the same unit across all available years - Calculate the year-over-year trend in draw_odds_percent (is it going up or down?) - Project odds for points+1, points+2, points+3 using the trend - Add to the unit result: { projection: { plus1yr: X%, plus2yr: X%, plus3yr: X% }, trend: &apos;tightening&apos; | &apos;loosening&apos; | &apos;stable&apos; } Update the Claude API system prompt to include the projection data. Add a follow-up question suggestion in the UI: &apos;What if I wait one more year?&apos; that pre-fills the chat input. ✓ Check: Ask Scout &apos;what if I wait one more year?&apos; The response references specific percentage changes in draw odds for the recommended units, not generic advice. 7.4 — Outfitter Cross-Reference The flywheel — Scout recommends units, then shows outfitters operating those units. Read CONTEXT.md first. Add outfitter cross-referencing to Scout results. After the scoring function runs and returns the top 10 units, query the listings table: - Find all active listings where states array contains the selected state AND species array contains the selected species - We don&apos;t have unit-level data on listings yet — for now, show all outfitters in that state/species combination - Return max 3 outfitter listings per unit Display in the Scout UI: - Below each recommended unit in the chat response, show a small &apos;Outfitters in this area&apos; section - Each outfitter card: business name, price, success rate, link to full listing - Label clearly: &apos;These outfitters operate in [State] for [Species] — verify they cover this specific unit&apos; Add a unit field to the listings table (text, optional) so outfitters can specify which units they operate in. Update the listing create form to include this field. ✓ Check: Run a Scout query. The AI response appears with outfitter cards below the top unit recommendations. Clicking an outfitter card links to their full listing page. Reusable Prompt Templates Copy and adapt these for common tasks that come up during the build. Add a New Filter to Search Read CONTEXT.md first. Add [FILTER NAME] as a filter on the /listings search page. The filter should: appear in the left sidebar, query the listings table where [COLUMN] matches the selection, update results without a page reload, and reset when the user clicks &apos;Clear filters&apos;. The column [COLUMN NAME] already exists in the listings table as [DATA TYPE]. Fix a Bug Read CONTEXT.md first. This error is happening in HuntScout when I [DESCRIBE WHAT YOU WERE DOING]: [PASTE THE EXACT ERROR MESSAGE AND STACK TRACE] Diagnose the cause, fix it, and explain what was wrong so I understand it. Add an Email Notification Read CONTEXT.md first. Add an email notification using Resend that triggers when [EVENT]. The email goes to [RECIPIENT — hunter or outfitter]. Subject: [SUBJECT] It should contain: [DESCRIBE CONTENT] Trigger it from: [API ROUTE OR SUPABASE WEBHOOK] Send from: notifications@huntscout.com Add a New Page Read CONTEXT.md first. Build a new page at [ROUTE]. Who can access it: [hunters only / outfitters only / public / admin only] What it does: [DESCRIBE THE PURPOSE] Data it needs from Supabase: [DESCRIBE THE QUERY] Main UI elements: [LIST WHAT SHOULD BE ON THE PAGE] Debug a Supabase Query Read CONTEXT.md first. This Supabase query isn&apos;t returning what I expect: [PASTE THE QUERY CODE] I expected: [DESCRIBE EXPECTED RESULT] I&apos;m getting: [DESCRIBE ACTUAL RESULT] Also check if the RLS policy on this table could be blocking the result. Deploy and Verify Read CONTEXT.md first. I want to deploy the latest changes to Vercel. Before deploying: 1. Check for any TypeScript errors: run tsc --noEmit 2. Check for any obvious issues that would cause a Vercel build to fail 3. Confirm all environment variables referenced in the code are in my .env.local list List any issues found. If none, tell me the git commands to push and confirm the deployment. Update CONTEXT.md After a Session Run this at the end of every productive session to keep the context file current. Read the current CONTEXT.md. Based on what we built in this session, update the Build Status checklist — mark completed items with [x]. Add any new decisions made during this session to the Decisions Made section. Show me the updated file content. Service Costs &amp; When to Upgrade Service Free Tier Paid Tier Upgrade When Vercel Unlimited hobby projects $20/mo Pro Need team members or &gt;100GB bandwidth/mo Supabase 500MB DB, 50K MAU, 1GB storage $25/mo Pro DB hits 500MB or monthly active users exceed 50K Claude API $5 free credit ~$0.003–0.015/1K tokens Budget $20–50/mo from Scout beta onward Stripe No monthly fee 2.9% + 30¢ per transaction Always proportional to revenue — no upgrade needed Resend 100 emails/mo $20/mo (50K emails) When booking/alert emails exceed 100/mo Cloudflare (domain) N/A ~$9/yr per domain Already paid — renews annually Realistic monthly hard cost at launch: $50–150/month. Claude API and Stripe fees are the only real variables — both scale directly with usage and revenue.
